/* ? Why does return not work in fibonacci body? */ 
let
  function fibonacci(n: int): int =
    if n <= 1 then
      n 
    else
      fibonacci(n - 1) + fibonacci(n - 2)
in
  for i := 1 to 8 do
    (
      print_int(fibonacci(i));
      print("\n")
    )
end
